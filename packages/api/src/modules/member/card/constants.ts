/**
 * 默认会员卡需要的经验
 */
export const DEFAULT_MEMBER_CARD_NEED_EXP = 100

/**
 * 默认会员卡折扣
 */
export const DEFAULT_MEMBER_CARD_DISCOUNT = 100

/**
 * 默认会员卡积分倍率
 */
export const DEFAULT_MEMBER_CARD_POINTS_RATIO = 1

/**
 * 会员徽章默认样式
 */
export const DEFAULT_MEMBER_BADGE_STYLE = {
  image: '',
  icon: '',
  textColor: '#000',
  bgColor: '#fff',
}

/**
 * 会员卡默认样式
 */
export const DEFAULT_MEMBER_CARD_STYLE = {
  image: '',
  icon: '',
  textColor: '#000',
  bgColor: '#fff',
  bgImage: '',
}
