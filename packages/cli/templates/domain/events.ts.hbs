import { AdminEvent } from '~/common/events'
import { MODULE_NAME } from '{{ moduleAlias }}/constants'

/**
 * Create {{ startCase domainName }} Event
 */
export class {{ domainName }}CreateEvent extends AdminEvent {
  constructor(
    public readonly {{ domain }}Id: number,
    public readonly {{ domain }}Name: string,
  ) {
    super(MODULE_NAME)
  }

  get logContent() {
    return `创建 - ${this.{{ domain }}Name}`
  }
}

/**
 * Update {{ startCase domainName }} Event
 */
export class {{ domainName }}UpdateEvent extends AdminEvent {
  constructor(
    public readonly {{ domain }}Id: number,
    public readonly {{ domain }}Name: string,
  ) {
    super(MODULE_NAME)
  }

  get logContent() {
    return `更新 - ${this.{{ domain }}Name}`
  }
}

/**
 * Delete {{ startCase domainName }} Event
 */
export class {{ domainName }}DeleteEvent extends AdminEvent {
  constructor(
    public readonly {{ domain }}Id: number,
    public readonly {{ domain }}Name: string,
  ) {
    super(MODULE_NAME)
  }

  get logContent() {
    return `删除 - ${this.{{ domain }}Name}`
  }
}
